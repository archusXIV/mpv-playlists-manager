#!/bin/bash
## This is a part of main script: mpm.

_GetLinkTitle() {
    case "$1" in
        -a) FILE="$AUDIO_TITLE" ;;
        -v) FILE="$VIDEO_TITLE" ;;
        -d) FILE=""$tmp_dir"/download.txt" ;;
    esac
    cat "$FILE" | sed -n '1p'
}
