#!/bin/bash
## This is a part of main script: mpm.

_EditPlaylist() {

    cd "$playlists_dir"

    PS3=${g}' Choose a playlist to edit: '${nc}

    select list in $(find . -name "*.m3u" -o -name "*.m3u_origin" \
        | sort -d | sed 's,.*/,,'); do
        __
        $EDITOR "$list"
        echo -e " ${g}$list saved${nc}\n"
        sleep 2
        read -rsn 1 -p ${g}' [C]ontinue editing, [B]ack to menu.'${nc} edit
        case "$edit" in
            c|C)
                __
                continue
                __
            ;;
            b|B)
                _Menu
            ;;
        esac
    done

}
